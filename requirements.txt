pymupdf>=1.24
python-docx>=1.1
pillow>=10.0

# Optional. Only used by the API-key engines; the Claude Code and Codex engines
# need neither of these, since they talk to the CLIs you already have.
anthropic>=1.0
openai>=1.0
